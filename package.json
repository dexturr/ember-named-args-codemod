{
  "name": "named-args-codemod",
  "version": "1.0.0",
  "description": "Moves hbs files to using named args",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/tests.js --timeout=50000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^5.1.0",
    "eslint-plugin-node": "^6.0.1",
    "mocha": "^5.2.0"
  },
  "dependencies": {
    "ember-template-recast": "^1.2.0",
    "globby": "^8.0.1"
  },
  "engines": {
    "node": "6.* || 8.* || >= 10.*"
  }
}
